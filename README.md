# media

Marketing assets for the README, kept on this orphan branch so they never
enter `main`'s history or a release. Re-record with `tools/demo` on `main`
and force-push here.

- `demo.mp4` – 1440×900 H.264 screen recording of the UI tour
- `demo.gif` – 960 px wide, 10 fps GIF of the same tour, embedded in the README
